from turtle import *

Luna = Turtle()
Lycra = Turtle()
Kassie = Turtle()
Wakasa = Turtle()
Saizo = Turtle()

def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):
        
def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):

def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):

def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):
