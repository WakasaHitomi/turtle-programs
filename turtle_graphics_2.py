from turtle import *

Luna = Turtle()
Lycra = Turtle()
Kassie = Turtle()
Wakasa = Turtle()
Saizo = Turtle()

def luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):
        lt(10)
        fd(40)
    end_fill()

speed(10)

luna_moon(300, -200, 0, "black", "azure3"
def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):
    pass

def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):
    pass

def Luna_Moon(x, y, points, line, fill):
    penup()
    goto(x, y)
    pendown()

    color(line, fill)

    begin_fill()
    for i in range(points):
    pass
